PORT=8080
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=change_me

# Twilio SMS (used when a phone logs in)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE=

# SMTP Email (used when an email logs in)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
FROM_EMAIL="Alluvo OTP <no-reply@example.com>"
